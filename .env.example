# Copy this file to .env and fill in real values. .env is gitignored — never commit real secrets.

# Used starting in Phase 4, for AI-based location/role classification of ambiguous postings.
ANTHROPIC_API_KEY=
